import React from "react";
import { View, Text, Button } from 'react-native';

export default function JoinSession({ navigation }){
    return (
        <View>
            <Text>JoinSession</Text>
        </View>
    );
}